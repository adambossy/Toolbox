" Syntax highlighting on
syntax on

" Turn on search color highlighting
set hlsearch

" Set tab width to 4
set smartindent
set tabstop=4
set softtabstop=4
set shiftwidth=4
" Various other options to try :set tabstop=4 softtabstop=4 shiftwidth=4 noexpandtab

" Show line numbers
set number

" Allow switching files without saving them
set hidden

" Clear highlighting from last search
nnoremap <esc> :noh<return><esc>
